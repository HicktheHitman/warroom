// js/fragcast.js — FragCast episode feed. Built in Step 7.
async function loadFragcastPage() {
  document.getElementById('fragcast-content').innerHTML = `
    <div class="card card-top">
      <div class="card-header">
        <div class="card-title">FRAGCAST // WEEKLY INTEL</div>
        <div style="font-family:var(--font-mono);font-size:9px;color:var(--olive-glow);">LIVE EVERY WEEK</div>
      </div>
      <div style="font-family:var(--font-mono);font-size:10px;color:var(--bone-dim);
                  padding:1rem 0;text-align:center;letter-spacing:1px;">
        COMING IN STEP 7
      </div>
    </div>`;
}
