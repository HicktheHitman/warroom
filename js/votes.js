// js/votes.js — Hype Meter + Weapon Prediction. Built in Step 5.
async function loadWeaponsPage() {
  document.getElementById('weapons-content').innerHTML = `
    <div class="card card-top">
      <div class="card-header">
        <div class="card-title">WEAPON PREDICTION DATABASE</div>
      </div>
      <div style="font-family:var(--font-mono);font-size:10px;color:var(--bone-dim);
                  padding:1rem 0;text-align:center;letter-spacing:1px;">
        COMING IN STEP 5
      </div>
    </div>`;
}
